import { Transformer, TransformerHandler } from '@pequehq/framework';

@Transformer()
export class {{{className}}} implements TransformerHandler {
  transform(value: unknown): unknown | Promise<unknown> {
    // Put your logic here.
    throw new Error('Method not implemented.');
  }
}
