{
  "name": "peque-test",
  "version": "1.0.0",
  "description": "Peque.ts Test utils",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "start": "ts-node src/index.ts",
    "clean": "rimraf .coverage dist",
    "test": "node --loader ts-node/esm ../../node_modules/uvu/bin.js src .test.ts",
    "lint": "eslint \"./{src,test}/**/*.ts\" --fix"
  },
  "author": "Simone Di Cicco",
  "license": "ISC",
  "dependencies": {
    "peque-di": "workspace:*"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
